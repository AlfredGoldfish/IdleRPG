/// <summary>
/// Minimal coin metal enum kept in global namespace for compatibility with existing scripts.
/// We can move this under IdleRPG.Core in a later refactor when CurrencyDef ScriptableObjects are introduced.
/// </summary>
public enum Metal
{
    Copper = 0,
    Silver = 1,
    Gold = 2,
    Platinum = 3
}
